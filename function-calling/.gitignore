# Secrets
.env
api_config.json

# Model outputs (LoRA adapter / merged model are published on Hugging Face, not tracked here)
saves/
output/
logs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# Evaluation outputs regenerated by re-running eval scripts outside results/
# (files already committed under results/ are the paper's reference outputs and are tracked)
/predictions_custom.json
/metrics.json
/bad_cases.json
*.pid

# Local IDE config
.claude/
